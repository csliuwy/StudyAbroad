# Optional: full API base (no trailing slash). Example when API is only on this host:
# VITE_API_BASE_URL=https://studyabroad.martxdata.com/api
#
# If unset: dev uses same hostname as the page with port 8080; prod build uses same-origin /api.
